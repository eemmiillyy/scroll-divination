  // Shape matching configuration.
  const RESAMPLE_COUNT = 128;
  const SVG_SAMPLE_COUNT = 512;
  const STAMP_BASE_SPACING = 10;
  const STAMP_SIZE = 18; // CSS pixels width

  export {
    RESAMPLE_COUNT,
    SVG_SAMPLE_COUNT,
    STAMP_BASE_SPACING,
    STAMP_SIZE,
  };