# To run this extension in your chrome browser:
1. git clone the repo
2. install deps with
```
npm i
```
3. build with
```
npm run build
```
4. Go to chrome://extensions/
5. Click "Load unpacked"
6. Select this repository folder